{
  "name": "aws_workshop",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sls-deploy": "SLS_DEBUG=* serverless deploy --aws-profile poc  --stage dev --region us-east-1 --verbose",
    "sls-print": "serverless print --aws-profile poc --stage dev --region us-east-1",
    "sls-remove": "SLS_DEBUG=* serverless remove --aws-profile poc --stage dev --region us-east-1 --verbose"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "serverless-python-requirements": "^4.0.4"
  }
}
